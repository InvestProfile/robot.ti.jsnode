version: '3.1'

services:
  robot:
    image: node:20.0.0-slim
    restart: always
    container_name: robot
    ports:
      - 5757:3000
    env_file: "./.env"
    command: bash -c 'npm run start'
    working_dir: /code
    volumes:
      - .:/code
